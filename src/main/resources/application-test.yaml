server:
  port: 8080

spring:
  application:
    name: "Auth Backend Test"
  datasource:
    driver-class-name: "org.h2.Driver"
    url: "jdbc:h2:mem:auth-test"
    username: "sa"

  jpa:
    database-platform: "org.hibernate.dialect.H2Dialect"
    hibernate:
      ddl-auto: "validate"

jwt:
  secret: secret
  expiration: 86400000
